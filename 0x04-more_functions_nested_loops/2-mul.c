#include "main.h"

/**
 * mul - multiplies two integgers
 * @x: first no. to multiply
 * @y: second no. to mltiply, with first
 * Return: the result of multiplication b/t x and param respctively
 */

int mul(int x, int y)

{
	return (x * y);
}
